import styled from "styled-components";

export const Container = styled.form`

	div {
		display: flex;
		align-items: center;

		img {
			width: 100%;
			max-width: 60px;
		}

		h3 {
			text-transform: uppercase;
			color: var(--green-500);
			margin-left: 1rem;
		}
	}
`